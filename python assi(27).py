#27.Write a program in python that converts a string into a list of its characters.
def string_to_list(string):
    return list(string)

# Example usage:
string = input("Enter a string: ")
print(f"The list of characters: {string_to_list(string)}")

